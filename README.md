CsvMetricsProcessor — это сервис для потоковой обработки и аналитики временных рядов (timescale-метрик). Проект спроектирован как масштабируемое решение для агрегации данных из внешних CSV-источников. В основе системы лежит Clean Architecture, что позволило изолировать сложную бизнес-логику статистических вычислений от механизмов хранения данных и внешних интерфейсов.

## 🚀 Ключевые особенности

- **Слоистая архитектура:** Четкое разделение на Domain, Application, Infrastructure и Presentation.
- **Гибкая фильтрация:** Система построения запросов позволяет комбинировать фильтры по именам файлов, временным диапазонам и значениям метрик.
- **Value Objects:** Использование доменных моделей для обеспечения валидности данных на всех этапах.

## 🛠 Технологический стек

- **Runtime:** .NET 10
- **Database:** PostgreSQL
- **API:** REST API с поддержкой Swagger/OpenAPI

## 🏗 Структура решения

- `Domain`: Ядро системы. Содержит сущности, Value Objects и логику статистических расчетов.
- `Application`: Сценарии использования (Use Cases). Здесь находятся сервисы и контракты.
- `Infrastructure`: Реализация работы с БД, репозитории.
- `Presentation`: Слой API (контроллеры и модели запросов).

## 📊 Математическая обработка
Сервис автоматически вычисляет для каждого загруженного файла:
- Среднее арифметическое и Медиану.
- Максимальное и Минимальное значения.
- Временную дельту выполнения операций.